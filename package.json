{
  "name": "mini-react",
  "description": "Implement a simple react",
  "scripts": {
    "dev:play": "pnpm --filter ./playground dev",
    "build:play": "pnpm --filter ./playground build",
    "preview:play": "pnpm --filter ./playground preview"
  },
  "dependencies": {
    "mini-react": "workspace:^",
    "mini-react-dom": "workspace:^"
  }
}
